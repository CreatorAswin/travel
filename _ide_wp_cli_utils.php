<?php
/**
 * IDE Compatibility Stubs for WP-CLI Utils
 */

namespace WP_CLI\Utils;

if ( ! function_exists( 'WP_CLI\Utils\format_items' ) ) {
    /**
     * @param string $format
     * @param array $items
     * @param array $fields
     */
    function format_items( $format, $items, $fields ) {}
}

if ( ! function_exists( 'WP_CLI\Utils\format_item' ) ) {
    /**
     * @param string $format
     * @param array $item
     * @param array $fields
     */
    function format_item( $format, $item, $fields ) {}
}
